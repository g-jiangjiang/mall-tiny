<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.tiny.modules.ums.mapper.UmsRoleDeptRelationMapper">

    <resultMap id="BaseResultMap" type="com.macro.mall.tiny.modules.ums.model.UmsRoleDeptRelation">
        <id column="id" property="id" />
        <result column="role_id" property="roleId" />
        <result column="dept_id" property="deptId" />
        <result column="create_time" property="createTime" />
    </resultMap>

    <delete id="deleteByRoleId">
        delete from ums_role_dept_relation
        where role_id = #{roleId}
    </delete>

    <insert id="batchInsert">
        insert into ums_role_dept_relation (role_id, dept_id, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId}, #{item.deptId}, NOW())
        </foreach>
    </insert>

    <select id="selectDeptIdsByRoleId" resultType="java.lang.Long">
        select dept_id from ums_role_dept_relation
        where role_id = #{roleId}
    </select>

</mapper>
