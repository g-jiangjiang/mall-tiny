<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.macro.mall.tiny.modules.ums.mapper.UmsRoleOrganizationRelationMapper">

    <resultMap id="BaseResultMap" type="com.macro.mall.tiny.modules.ums.model.UmsRoleOrganizationRelation">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="role_id" property="roleId" jdbcType="BIGINT"/>
        <result column="organization_id" property="organizationId" jdbcType="BIGINT"/>
        <result column="scope" property="scope" jdbcType="INTEGER"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <select id="getOrganizationIdsByRoleId" resultType="java.lang.Long">
        SELECT DISTINCT organization_id
        FROM ums_role_organization_relation
        WHERE role_id = #{roleId}
    </select>

    <select id="getOrganizationIdsByAdminId" resultType="java.lang.Long">
        SELECT DISTINCT ro.organization_id
        FROM ums_admin_role_relation ar
        JOIN ums_role_organization_relation ro ON ar.role_id = ro.role_id
        WHERE ar.admin_id = #{adminId}
    </select>

    <insert id="insertBatch">
        INSERT INTO ums_role_organization_relation (role_id, organization_id, scope, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.roleId}, #{item.organizationId}, #{item.scope}, #{item.createTime})
        </foreach>
    </insert>

</mapper>